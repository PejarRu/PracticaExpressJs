{% extends "publico_base.njk" %}

{% block title %}Iniciar sesión{% endblock %}

{% block contenido %}
    <div class="container col-6 mt-4">

        <form action="{{ sendTo |default('/auth/login') }}" method="post">
            {% if error %}
                <div class="alert alert-danger" role="alert">
                    {{ error }}
                </div>
            {% endif %}

            <div class="form-group">
                <label for="login">Correo electrónico</label>
                <input type="text" name="login" id="login" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" name="password" id="password" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary">{{action | default('Iniciar sesión')}}</button>
            {% if action === 'Registrarme' %}
                <a class="float-right" href="/auth/login" class="btn btn-link">Ya estoy registrado</a>
            {% else %}
                <a class="float-right" href="/auth/register" class="btn btn-link">Registratme</a>
            {% endif %}
        </form>

    </div>
{% endblock %}